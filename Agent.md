# Agent.md

适用项目：`macOS AI 语音输入 helper app`

当前方向：`极致创新，但必须可实现；先做 helper app，不做 InputMethodKit；先接云端 API，用户前端配置自己的 Key；历史/对话默认保留本地`

## 使用方法

这 10 条 prompt 不是普通待办清单，而是按真实软件工程推进顺序拆开的执行指令。

使用方式：

1. 按顺序发到新的聊天框里，不要跳步。
2. 每次都让模型先检查当前仓库状态，再继续。
3. 每一轮都先想：这一次怎样做出一个真正有辨识度的点。
4. 然后判断：这个点今天能不能真的做出来。
5. 每完成一个小步就 `commit`。
6. 能自己做的都自己做，除非遇到 GitHub / Apple 账号 / 签名这类硬阻塞。

每一轮结尾都必须给出：

- 做了什么
- 没做什么
- 当前风险
- 下一步最小动作
- 本轮 commits

这 10 条 prompt 的目标不是做一个普通工具，而是做一个：

`键盘优先、跨应用、上下文感知、选区可改写、支持自定义模型、尽量本地优先的 macOS AI 语音输入产品`

## 通用硬约束

1. 产品形态必须是 `macOS helper app`，不是 `InputMethodKit` 输入法。
2. 目标是键盘优先、系统级可随时唤起的 AI 语音输入工具。
3. 第一阶段使用各大模型厂商云端 API；用户在前端输入自己的 API Key 来接入。
4. 历史记录、对话数据、配置数据默认保留在本地。
5. 需要有快捷键唤醒和快捷键取消当前会话。
6. 完整按软件工程流程推进，每完成一个小步就 `commit`。
7. 能自己完成的都自己完成；除非遇到 GitHub 登录、Apple 签名等硬阻塞，否则不要停下来问用户。
8. 每一轮都要同时兼顾创新与可实现度，不要做平庸样板，也不要做飘在天上的概念稿。

---

## Prompt 1：立项、建库、定北极星、定工程节奏

目标：

1. 建 GitHub 仓库
2. 初始化 macOS 原生工程
3. 建立软件工程计划
4. 把“创新但可落地”的原则写进项目文档

```text
你现在在开发一个全新的 macOS AI 语音输入 helper app。项目目录是：

/Users/zhuanghongkai/Desktop/颠覆性 AI 语音输入法

硬性约束：
1. 产品形态是 `macOS helper app`，不是 `InputMethodKit` 输入法。
2. 目标是键盘优先、系统级可随时唤起的 AI 语音输入工具。
3. 第一阶段使用各大模型厂商云端 API；用户在前端输入自己的 API Key 来接入。
4. 历史记录、对话数据、配置数据默认保留在本地。
5. 需要有快捷键唤醒和快捷键取消当前会话。
6. 要求完整软件工程流程推进，每完成一个小步就 commit。
7. 能自己完成的都自己完成；除非遇到 GitHub 登录、Apple 签名等硬阻塞，否则不要停下来问我。

本轮目标不是只做“空工程”，而是建立一个真正能支撑产品从 0 到 1 的基础盘。

请按下面顺序执行：

A. 先检查当前目录、现有文档、git 状态、是否已有 Xcode 工程、是否已有 GitHub 远程。
B. 如果没有 git 仓库，初始化 git。
C. 如果没有 GitHub 仓库，使用 gh 创建一个新的 private repo，并把本地仓库连上。
D. 初始化一个原生 macOS 项目，优先使用 Swift + SwiftUI，并确保它适合演进为 menu bar / helper app。
E. 在仓库中建立最基本但专业的文档体系，至少包含：
   - README.md
   - docs/product-principles.md
   - docs/engineering-plan.md
   - docs/architecture.md
   - docs/milestones.md
   - docs/adr/0001-helper-app-direction.md
F. 关键要求：不要把项目做成平庸样板。请先提出 3 个“有野心但可在 v1 验证”的产品创新方向，例如：
   - 键盘交互方式创新
   - 选区改写的交互创新
   - 场景感知与写作风格切换创新
   对每个方向给出：
   - 用户价值
   - 技术复杂度
   - 风险
   - 是否适合 v1
   然后选出 1 到 2 个写入 docs/product-principles.md，作为项目的创新主线。
G. 在 docs/engineering-plan.md 里明确：
   - 已确认范围
   - 暂不做的范围
   - 分阶段目标
   - 每阶段通过标准
   - 风险清单
   - 提交策略
   - 测试与验证策略
H. 确保工程能在本地 build 成功。
I. 把初始成果 push 到 GitHub。

执行要求：
1. 每完成一个小步就 commit，不要一次提交很大一坨。
2. commit message 要清楚，体现工程意图。
3. 不要为了快而省略文档。
4. 不要把“创新”写成空话，必须兼顾可实现程度。
5. 在最终汇报里给我：
   - 仓库地址
   - 当前文档结构
   - 选中的创新主线
   - 已完成的 commits
   - 下一步最合理的最小动作
```

---

## Prompt 2：设计有记忆点的交互体系，再落成 helper app 骨架

目标：

1. 不只做 app 壳子，而是先把交互语言定出来
2. 同时把 helper app 的基础结构搭稳

```text
继续当前仓库，先检查 git status、最近 commits、README、engineering plan，再继续。不要重复做已经完成的工作。

本轮目标：
把产品从“只有仓库和工程”推进到“有明确交互语言的 helper app 骨架”。

重要要求：
这个产品要有记忆点，不要只是一个普通菜单栏工具。请在可实现范围内做出一种明显区别于普通 dictation app 的交互骨架。

请按以下步骤推进：

A. 先基于现有文档，提出 3 套键盘优先的交互体系候选。每套至少要包含：
   - 唤醒方式
   - 停止方式
   - 取消方式
   - “普通听写”和“选区改写”的分流方式
   - 是否需要短暂 HUD / 状态浮层
   对每套方案都评估：
   - 学习成本
   - 系统冲突风险
   - 开发复杂度
   - 对创新感的贡献
B. 选出 1 套最适合 v1 的交互骨架，并写入 docs/product-principles.md 和 docs/architecture.md。
C. 把 app 组织成适合长期演进的结构，至少明确这些模块：
   - App lifecycle
   - Session state
   - Hotkey
   - AudioCapture
   - SpeechProvider
   - TextOutput
   - ContextDetection
   - Permissions
   - LocalStore
   - Settings
   - Diagnostics
D. 实现可运行的 helper app 形态：
   - 菜单栏图标
   - 基础菜单
   - 设置入口
   - 退出入口
E. 建立统一状态机，至少覆盖：
   - idle
   - listening
   - transcribing
   - rewriting
   - inserting
   - cancelled
   - error
F. 如果合适，请实现一个极简但有辨识度的状态反馈组件，例如：
   - 短暂浮层
   - 菜单栏状态变化
   - 非侵入式状态条
   目标不是花哨，而是让交互显得“像一个认真设计过的产品”。
G. 确保工程可 build、可运行。

执行要求：
1. 本轮要同时兼顾创新与可落地，不要做炫技 UI。
2. 不要提前接 API。
3. 不要提前实现复杂权限逻辑。
4. 每完成一个小步就 commit。
5. 最终汇报时给我：
   - 最终采用的交互骨架
   - 为什么它比另外两套更适合
   - 当前工程结构
   - 本轮 commits
   - 还缺哪些核心能力
```

---

## Prompt 3：全局快捷键、取消机制、权限中心、状态反馈

目标：

1. 做出第一条真正像“系统工具”的交互闭环
2. 让 app 从静态骨架变成可被唤起的工具

```text
继续当前仓库，先检查现有 app 骨架、状态机、文档和最近 commits，避免重复劳动。

本轮目标：
实现这个产品最核心的系统能力基础：
1. 全局快捷键唤醒
2. 全局快捷键停止/提交当前会话
3. 全局快捷键取消当前会话
4. 权限检测与引导
5. 清晰的状态反馈

创新要求：
不要只做“能用”。请在本轮里做一个小而锋利、但明显提升高级感的设计点，例如：
   - 更清晰的会话状态切换反馈
   - 更低打扰的 HUD
   - 更聪明的快捷键冲突提示
请先提出 2 到 3 个候选，评估成本后只实现 1 个，不要贪多。

请完成以下工作：

A. 选择一个成熟可靠的全局快捷键方案，优先使用成熟库；如果需要更底层能力，再说明理由。
B. 先实现一版默认快捷键体系，至少包含：
   - 唤醒/开始
   - 停止并进入后续处理
   - 取消当前会话
C. 在设置页里做快捷键配置的基础入口。
D. 建立统一权限中心，检测并展示：
   - 麦克风权限
   - Accessibility 权限
   - 如果方案需要，再处理键盘监听相关权限
E. 在 UI 中明确展示权限状态、缺失原因和引导路径。
F. 让状态反馈和状态机打通。
G. 文档中记录：
   - 快捷键策略
   - 权限模型
   - 已知系统限制

执行要求：
1. 先追求稳定，不要过早挑战复杂修饰键单击/长按识别。
2. “取消”是取消当前会话，不是退出整个 app。
3. 菜单中保留 Quit App。
4. 每完成一个小步就 commit。
5. 每次关键改动后尽量 build 验证。

最终汇报请包括：
1. 当前默认快捷键是什么
2. 状态反馈做了什么创新点
3. 权限中心当前覆盖了哪些权限
4. 已知风险
5. 本轮 commits
```

---

## Prompt 4：本地录音链路 + 让用户一开口就看到反馈的会话体验

目标：

1. 让用户第一次真正说起来
2. 从工具感升级到产品感

```text
继续当前仓库。本轮只聚焦录音与语音会话体验，暂时不要接改写和复杂上下文。

目标：
打通这条链路：
快捷键唤醒 -> 开始录音 -> 实时反馈正在听 -> 停止录音 -> 形成待转写音频

创新要求：
普通录音不够。请在可实现范围内做出一个“让用户感觉产品活着”的反馈设计，例如：
   - 极简波形/音量反馈
   - 录音时的状态脉冲
   - 菜单栏图标动态变化
先提出 2 个候选，评估实现成本后只做 1 个。

请完成以下工作：

A. 使用 macOS 原生音频能力实现稳定录音。
B. 把录音链路和 SessionState 打通。
C. 支持：
   - 唤醒后开始录音
   - 再次触发停止录音
   - 取消会话并丢弃录音
D. 设计临时音频文件或内存缓冲策略，并说明：
   - 存储位置
   - 生命周期
   - 清理策略
E. 实现一个轻量但有辨识度的 listening 反馈。
F. 为录音失败、权限缺失、用户取消等情况加上清晰状态处理。
G. 更新 docs，记录音频会话设计和清理策略。

执行要求：
1. 本轮结束后，即使还没接 API，也要能完整跑一轮录音会话。
2. 临时文件不能堆积。
3. 反馈要克制，不能打扰用户当前应用。
4. 每完成一个小步就 commit。

最终汇报请包括：
1. 录音链路现在到哪一步
2. 当前 listening 反馈长什么样
3. 音频存储与清理策略
4. 本轮 commits
```

---

## Prompt 5：用户自填 API Key + 第一家云端转写 Provider

目标：

1. 做出第一条真实有价值的链路
2. 从“会录音”变成“会把语音变成文字”

```text
继续当前仓库，先确认录音链路已经稳定，再接云端转写。

产品约束：
1. 当前阶段走云端模型 API。
2. 用户必须能在前端设置里输入自己的 API Key。
3. Key 必须安全保存在本地，不允许普通明文配置乱放。
4. 架构上必须为多 provider 扩展留口子。

本轮目标：
打通第一条完整主链路：
快捷键唤醒 -> 录音 -> 停止 -> 上传音频 -> 云端转写 -> 获得文本结果

创新要求：
不要只做“请求成功”。请在本轮加入一个能够明显提升用户信任感的设计点，例如：
   - 清楚显示当前使用的是哪家 provider
   - 请求中状态可见
   - 转写失败后给出可操作建议
先评估 2 个方案，只实现 1 个最值得做的。

请完成以下工作：

A. 设计并实现：
   - SpeechTranscriptionProvider 协议
   - Provider 配置模型
   - API 凭证管理
B. 使用系统安全机制保存 API Key。
C. 在设置页中增加：
   - Provider 选择
   - API Key 输入
   - model 名称或相关可选参数
   - 基础校验
D. 先选择一家最适合作为首发的 provider，实现最稳的一版转写。
E. 打通录音结束后的转写流程。
F. 处理这些错误：
   - Key 缺失
   - 网络错误
   - Provider 返回错误
   - 音频格式不匹配
G. 文档中写清楚：
   - 首个 provider 选择理由
   - Key 保存策略
   - Provider 扩展点

执行要求：
1. 不要一次实现多家，先把第一家做稳。
2. 但接口必须支持以后接更多厂商。
3. 每完成一个小步就 commit。
4. 本轮结束后要能真实跑通一轮转写。

最终汇报请包括：
1. 当前接的是哪家 provider
2. 为什么先选它
3. 用户如何填写 Key
4. 完整链路是否已跑通
5. 本轮 commits
```

---

## Prompt 6：文本写回引擎 + 焦点输入框检测 + 第一版兼容性矩阵

目标：

1. 把“看到转写结果”推进到“真正写进当前应用”
2. 这是 helper app 成败的核心步骤

```text
继续当前仓库。本轮聚焦“把结果写回当前焦点位置”。

目标：
实现以下能力：
1. 普通听写结果写入当前输入位置
2. 能识别当前前台应用和焦点编辑目标
3. 对失败场景有明确 fallback 和记录

创新要求：
这一步虽然偏底层，但也不能只做粗糙工程。请在本轮加入一个实用创新点，例如：
   - 在用户提交前短暂预览转写结果
   - 针对写入失败给出更人性化 fallback
   - 对不同 app 展示不同写入策略提示
先提出 2 个候选，只选 1 个真正落地。

请完成以下工作：

A. 设计 TextOutput 层，至少区分：
   - 插入文本
   - 替换选中文本
   - 错误类型
   - fallback 路径
B. 实现当前前台应用与焦点上下文的基础识别，至少拿到：
   - app name
   - bundle id
   - 是否存在可编辑目标
C. 实现普通听写结果写回。
D. 如果需要 fallback，可设计剪贴板/粘贴 fallback，但必须明确标记为 fallback。
E. 对失败情况做清晰提示和日志。
F. 手测至少几类不同应用，并整理为第一版兼容性矩阵。
G. 更新文档，诚实记录：
   - 主路径是什么
   - fallback 是什么
   - 哪些 app 已验证
   - 哪些场景不稳定

执行要求：
1. 不要夸大兼容性。
2. 不要把 fallback 伪装成主路径。
3. 本轮结束后，至少要在 1 到 2 个常见 app 中做到“说完就写进去”。
4. 每完成一个小步就 commit。

最终汇报请包括：
1. 当前主写回路径
2. fallback 路径
3. 已测 app 列表
4. 已知不稳定点
5. 本轮 commits
```

---

## Prompt 7：做出产品灵魂能力，选区改写、翻译、结构化整理

目标：

1. 让产品从“语音输入工具”变成“AI 输入增强器”
2. 做出真正能打动人的第二条核心链路

```text
继续当前仓库。本轮做产品最有辨识度的能力之一：

用户选中一段文字 -> 按快捷键 -> 说出命令 -> AI 改写/翻译/整理 -> 覆盖原文

例如：
- 翻译成日语
- 更正式一点
- 更口语一点
- 精简一下
- 分点整理

创新要求：
这一轮不能只做“把 prompt 发给模型”。请先提出 3 种交互形态候选，例如：
   - 用户说自由命令
   - 常用动作模式化
   - 自由命令 + 常用动作快捷模式混合
评估：
   - 用户理解成本
   - 开发复杂度
   - 产品高级感
   - 是否适合 v1
然后选一个最适合当前阶段的方案落地。

请完成以下工作：

A. 设计 RewriteProvider / TextGenerationProvider 抽象。
B. 区分两种模式：
   - 无选区：普通听写
   - 有选区：进入“语音命令改写”链路
C. 设计清晰、可维护的 prompt/request 结构。
D. 实现至少这些动作：
   - 翻译
   - 润色
   - 精简
   - 结构化整理
E. 改写完成后覆盖原文。
F. 对这些失败场景做明确处理：
   - 没有选区
   - 指令为空
   - 模型失败
   - 覆盖失败
G. 把这条能力写进产品文档和架构文档。

执行要求：
1. 这条链路要像产品功能，不要像内部 demo。
2. 先做闭环，再做复杂 prompt 工程。
3. 所有记录仍默认保存在本地。
4. 每完成一个小步就 commit。

最终汇报请包括：
1. 当前支持哪些选区动作
2. 最终采用的交互方案
3. 无选区和有选区的行为差异
4. 本轮 commits
```

---

## Prompt 8：多 Provider、自定义 API、模型角色分工

目标：

1. 把产品从“能用一家的 demo”升级成“能接多家的平台”
2. 这是核心差异点之一

```text
继续当前仓库。本轮目标是把产品从单一 provider 升级为可扩展的多 provider 系统。

产品约束：
1. 这是一个重要差异点：用户既可以未来使用完整服务，也可以现在直接填写自己的 API Key。
2. 当前阶段先把“用户自带 API”做好。
3. 要支持多厂商，但不要求一次做完所有定制接入。

创新要求：
这一步不要只做“多几个下拉框”。请在可实现范围内加入一个真正让高级用户眼前一亮的能力，例如：
   - 转写 provider 和改写 provider 分离
   - 每种模式可绑定不同模型
   - OpenAI-compatible 的灵活接入
先提出 2 到 3 个候选，评估后实现 1 个最有价值的。

请完成以下工作：

A. 梳理现有 provider 抽象，拆分为至少两层：
   - SpeechTranscriptionProvider
   - TextGenerationProvider
B. 在设置页实现 provider 配置中心，至少包括：
   - provider 类型
   - API Key
   - base URL（可选）
   - model name（可选）
   - 启用/停用
C. 设计“最少定制覆盖最多厂商”的实现路线。
D. 如果合理，优先支持：
   - OpenAI-compatible 路线
   - 单独补充非兼容厂商
E. 让用户可以选择：
   - 哪家负责转写
   - 哪家负责改写
F. 做好错误提示、表单校验和配置持久化。
G. 文档里写清楚：
   - 当前支持的 provider 矩阵
   - 原生接入与兼容接入的区别
   - 新增 provider 的方法

执行要求：
1. 以扩展性优先，不以“一次接最多家”为目标。
2. 前端配置必须可用，不能只靠修改配置文件。
3. 每完成一个小步就 commit。
4. 保持现有主链路不被破坏。

最终汇报请包括：
1. 当前 provider 矩阵
2. 用户如何切换转写与改写 provider
3. 本轮实现的创新点是什么
4. 本轮 commits
```

---

## Prompt 9：本地历史、按应用策略、场景感知的第一层

目标：

1. 做出“会因场景而异”的产品差异
2. 同时把本地优先的隐私路线真正落地

```text
继续当前仓库。本轮目标是实现产品的重要差异化能力：

1. 所有历史/对话记录默认保留本地
2. 能识别用户是在哪个 app 中触发
3. 能按应用场景切换默认风格或行为

创新要求：
不要把“场景感知”做成只有 bundle id 的底层能力。请在可实现范围内加入一个能被用户真实感知到的设计点，例如：
   - 某些 app 默认更正式
   - 某些 app 默认更口语
   - 某些 app 默认更结构化
   - 某些 app 进入“选区改写优先”模式
先提出候选方案，评估后实现 1 个最有价值的第一版。

请完成以下工作：

A. 实现本地历史记录系统，至少记录：
   - 时间
   - 模式（普通听写 / 选区改写）
   - frontmost app
   - 输入文本 / 输出文本
   - provider 信息
   - 成功/失败状态
B. 历史数据默认本地保存，并支持删除。
C. 给用户提供最基础的历史查看入口。
D. 实现第一版场景感知：
   - frontmost app name
   - bundle id
E. 设计并实现“按应用策略”的最小版本，例如：
   - 默认语气
   - 默认输出风格
   - 默认是否结构化
F. 在设置页提供基础策略配置入口。
G. 文档中写清楚：
   - 本地存储结构
   - 隐私边界
   - 场景感知当前粒度
   - 未来扩展方向

执行要求：
1. 先做应用级场景感知，不要一开始做页面语义级。
2. 不要偷偷上云。
3. 历史数据要可管理、可删除。
4. 每完成一个小步就 commit。

最终汇报请包括：
1. 历史记录当前长什么样
2. 当前场景感知粒度
3. 用户能配置哪些按应用策略
4. 本轮 commits
```

---

## Prompt 10：打磨成一个能被试用的产品版本

目标：

1. 不是继续堆功能，而是形成一个像样的可交付版本
2. 让这个项目从技术拼装进入产品状态

```text
继续当前仓库。本轮目标是把项目从“很多功能已经存在”推进到“像一个真正可试用的产品版本”。

请完成以下工作：

A. 全面回看当前实现，优先处理最影响试用体验的问题：
   - 状态错乱
   - 权限异常
   - provider 配置错误
   - 转写失败
   - 文本写回失败
   - 选区改写失败
B. 补基础测试：
   - 纯逻辑单测
   - 状态机测试
   - provider 适配测试
C. 建立最小 CI，至少能在 GitHub 上跑 build / test。
D. 打磨用户体验：
   - 菜单和设置页信息架构
   - 权限提示文案
   - 错误信息
   - 历史查看体验
   - 状态反馈体验
E. 产出清晰的安装和使用文档：
   - README
   - 安装说明
   - 使用说明
   - API Key 配置说明
   - 权限说明
   - 已知兼容性问题
F. 输出一份发布前检查清单。
G. 如果条件允许，准备本地 release build。
H. 最后补一份 v1 backlog，把“现在不做但以后值得做的创新”单独整理出来。

创新要求：
本轮不是只修 bug。请在不冒大风险的前提下，补 1 个能明显提升产品完成度的小创新点，例如：
   - 更顺滑的状态反馈过渡
   - 历史记录中的场景筛选
   - 失败后的快速重试入口
先评估 2 个候选，只做 1 个。

执行要求：
1. 目标是形成一个明确的 v0/v1 可试用版本，而不是无限打磨。
2. 对不能完成的部分，要明确写出阻塞点和原因。
3. 每完成一个小步就 commit。
4. 最终要给出“当前版本能做什么、不能做什么、如何本地运行”的清晰总结。

最终汇报请包括：
1. 当前版本已经能做什么
2. 当前版本还不能做什么
3. 如何安装和试用
4. 当前 GitHub 仓库状态
5. 本轮 commits
```

---

## 这套方法到底在逼什么

这 10 条 prompt 的核心，不是“把功能做完”，而是一直逼着执行者同时做到两件事：

1. 每一轮都尝试做出一个有辨识度的创新点。
2. 但每一轮只落地那个最值得、今天最做得出来的点。

如果一句话概括这 10 条 prompt 的方法论，就是：

`每一轮都先追求产品锋芒，再用工程现实把它压到今天就能交付的版本。`
