# 魔法师 功能产品需求文档（PRD）

## 1. 文档目标
本文面向产品、设计、研发、测试，讲清楚“魔法师”功能的定位、页面布局、交互规则、能力边界、版本节奏。

## 2. 背景与问题
在写作、聊天、资料整理场景里，用户经常要做这些动作：
1. 改写一段话。
2. 把一句话拿去搜索。
3. 从一句话直接建日程。
4. 把内容记到备忘录。
5. 生成邮件草稿。

现状是动作分散在不同 App，步骤多、切换频繁、容易打断思路。

## 3. 产品目标
1. 用户只需“选中文字 + 下指令”，即可完成动作。
2. 五个动作统一入口，学习成本低。
3. 每个动作独立开关，独立权限引导，用户可按需启用。
4. 文案尽量白话，不讲实现细节。

## 4. 功能范围（v1）
1. `文字处理`：翻译、润色、扩写、精简、纠错。
2. `快速搜索`：在 Chrome 打开 Google 搜索结果页。
3. `一键建日程`：从选中内容推断时间与主题，直接加入日历。
4. `写入备忘录`：把选中内容写入备忘录。
5. `邮件草稿`：把选中内容整理成邮件草稿并打开邮件 App。

## 5. 不在 v1 范围
1. 不做“自动发送邮件”。
2. 不做“自动浏览网页并提炼结论”（先只做快速搜索）。
3. 不做复杂多轮追问交互（建日程按默认规则一次完成）。

## 6. 页面布局
### 6.1 入口
1. 右侧列表新增一级项：`魔法师`。

### 6.2 页面结构
1. 顶部标题：`魔法师`。
2. 顶部副标题：`选中文字，下指令，立刻执行。`
3. 下方展示 5 张功能卡片。
4. 每张卡片右侧放现有蓝色胶囊开关。

### 6.3 卡片信息
每张卡片固定展示：
1. 功能名。
2. 一句话说明（做什么）。
3. 边界说明（不做什么）。
4. 示例说法（用户可直接模仿）。
5. 状态标签：`未开启 / 需权限 / 已开启`。

## 7. 五个功能的用户文案（轻松亲和版）
### 7.1 文字处理
- 说明：把选中内容改成你想要的表达。
- 能力：翻译、润色、扩写、精简、纠错。
- 边界：仅作用于当前选中内容。
- 示例：`翻成日语`、`帮我润色一下`。

### 7.2 快速搜索
- 说明：把选中内容直接放进 Chrome 的 Google 搜索。
- 能力：一键打开结果页。
- 边界：只打开结果页，不自动浏览网页。
- 示例：`帮我搜索一下`。

### 7.3 一键建日程
- 说明：从选中内容里识别时间和主题，直接加入日历。
- 能力：自动生成标题、时间、备注。
- 边界：信息不完整时按默认规则自动推断。
- 示例：`周五下午和产品开评审会`。

### 7.4 写入备忘录
- 说明：把选中内容快速记到备忘录。
- 能力：新增一条笔记内容。
- 边界：只新增，不改已有笔记。
- 示例：`记到备忘录`。

### 7.5 邮件草稿
- 说明：把选中内容整理成邮件草稿，并打开邮件 App。
- 能力：自动填好主题和正文。
- 边界：只生成草稿，不自动发送。
- 示例：`整理成邮件草稿`。

## 8. 开关与权限交互
1. 用户点击某功能开关。
2. 若无需额外权限：立即启用，并提示“已开启”。
3. 若需要权限：弹出引导窗口，说明用途与影响。
4. 用户完成授权后，自动回到页面并启用该功能。
5. 若授权未完成，开关保持关闭，并提示原因。

## 9. 交互主流程（用户视角）
1. 用户在任意输入区选中文字。
2. 用户按住快捷键并说一句话。
3. 系统识别意图并执行对应动作。
4. 页面或系统 App 给出结果反馈（如已替换、已打开搜索、已建日程）。

## 10. 成功指标（上线后观测）
1. 五个开关启用率。
2. 动作执行成功率。
3. 从指令到结果的平均耗时。
4. 权限引导完成率。
5. 用户满意度评分（设置页或问卷）。

## 11. 版本节奏
### 阶段 A
先完成 `文字处理` 全链路（含替换与撤销能力）。

### 阶段 B
再接入 `快速搜索 / 一键建日程 / 写入备忘录 / 邮件草稿` 四个动作。

## 12. 风险与兜底
1. 文本获取在少数 App 可能失败：给出明确提示，建议先在可编辑文本区操作。
2. 日程时间推断可能偏差：按默认规则落地，并在结果提示里展示关键字段。
3. 备忘录依赖 Shortcuts 配置：缺配置时给引导步骤。
4. 邮件客户端未配置账号：提示先完成邮箱账号配置。
