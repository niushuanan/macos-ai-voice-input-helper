# 魔法师 功能软件工程文档

## 1. 目标与范围
本文面向研发与测试，定义“魔法师”v1 的技术实现路径。

v1 固定五个动作：
1. `text_transform`
2. `web_search`
3. `create_event`
4. `create_note`
5. `compose_email_draft`

## 2. 架构总览（四层）
1. `Selection Layer`：获取当前选中内容。
2. `Intent Layer`：把用户口令解析成结构化意图。
3. `Tool Layer`：按意图调用对应系统能力。
4. `Apply Layer`：把执行结果反馈到 UI 或系统 App。

建议数据流：
`SelectedText -> IntentJSON -> ToolRequest -> ToolResult -> UI Feedback`

## 3. 模块划分建议
建议新增目录：

```text
Magician/
  UI/
    MagicianView.swift
    MagicianFeatureCard.swift
    MagicianPermissionSheet.swift
  Domain/
    MagicianIntent.swift
    MagicianAction.swift
    MagicianState.swift
    MagicianError.swift
  Selection/
    SelectionProvider.swift
    ServicesSelectionProvider.swift
    AccessibilitySelectionProvider.swift
  Intent/
    IntentRouter.swift
    LLMIntentRouter.swift
    IntentSchemaValidator.swift
  Tools/
    ToolExecutor.swift
    TextTransformAdapter.swift
    WebSearchAdapter.swift
    EventAdapter.swift
    NoteAdapter.swift
    MailDraftAdapter.swift
  Apply/
    ResultApplier.swift
    TextReplaceApplier.swift
    ToastApplier.swift
  Permissions/
    PermissionCenter.swift
    PermissionPolicy.swift
  Analytics/
    MagicianMetrics.swift
  Tests/
    Unit/
    Integration/
    UI/
```

## 4. 核心数据结构
### 4.1 Intent Schema

```json
{
  "intent": "text_transform | web_search | create_event | create_note | compose_email_draft",
  "confidence": 0.0,
  "sourceText": "string",
  "params": {
    "mode": "translate | polish | expand | shorten | fix",
    "targetLanguage": "ja",
    "tone": "neutral",
    "query": "string",
    "title": "string",
    "startAt": "ISO8601",
    "endAt": "ISO8601",
    "location": "string",
    "noteBody": "string",
    "mailTo": ["a@example.com"],
    "mailSubject": "string",
    "mailBody": "string"
  }
}
```

### 4.2 Swift 协议建议

```swift
protocol SelectionProvider {
    func currentSelection() async throws -> String
}

protocol IntentRouter {
    func route(command: String, selection: String) async throws -> MagicianIntent
}

protocol ToolExecutor {
    func execute(intent: MagicianIntent) async throws -> MagicianResult
}

protocol ResultApplier {
    func apply(_ result: MagicianResult) async
}
```

## 5. 四层实现细节
### 5.1 Selection Layer
1. 主实现：`ServicesSelectionProvider`。
2. 兜底实现：`AccessibilitySelectionProvider`。
3. 返回空文本时直接抛错：`selection_empty`。

### 5.2 Intent Layer
1. 输入：用户语音转文本 + 选中文本。
2. 输出：标准 `MagicianIntent`。
3. 要求：
- 非法 JSON 直接失败。
- `intent` 必须在白名单。
- `create_event` 不走二次追问，缺字段走默认值推断。

### 5.3 Tool Layer
#### text_transform
1. 输入：`mode + sourceText + optional params`。
2. 输出：`previewText + finalText`。
3. 要求：支持替换前预览与撤销。

#### web_search
1. 只做快速模式。
2. 生成 URL：`https://www.google.com/search?q=<encoded>`。
3. 优先尝试打开 `Chrome`；不可用时降级默认浏览器。

#### create_event
1. 使用 `EventKit`。
2. 权限流程：检查状态 -> 申请授权 -> 创建 `EKEvent`。
3. 默认规则（可配置）：
- 未给时长：默认 60 分钟。
- 未给标题：使用选中内容前 20 字。

#### create_note
1. 通过 `Shortcuts` 调用预设快捷指令。
2. 支持两种方式：
- `shortcuts run <name>`
- `shortcuts://run-shortcut?...`
3. 若快捷指令缺失：返回 `shortcut_not_found` 并给引导文案。

#### compose_email_draft
1. 主路径：`NSSharingService(named: .composeEmail)`。
2. 兜底路径：`mailto:`。
3. 只生成草稿，不触发发送。

### 5.4 Apply Layer
1. text_transform：显示预览 -> 用户确认 -> 覆盖原文本 -> 提供 Undo。
2. 其他动作：显示 Toast（成功/失败），并记录埋点。

## 6. 权限矩阵
1. `text_transform`：文本访问相关权限（按现有工程能力接入）。
2. `web_search`：通常无需额外权限。
3. `create_event`：日历权限（EventKit）。
4. `create_note`：Shortcuts 可用性与目标快捷指令存在性。
5. `compose_email_draft`：通常无需额外权限；若无邮箱账号，给配置提示。

## 7. 错误模型
建议统一：`code + userMessage + debugMessage + recoverAction`

建议错误码：
1. `selection_empty`
2. `permission_denied`
3. `intent_parse_failed`
4. `tool_execution_failed`
5. `event_create_failed`
6. `shortcut_not_found`
7. `mail_unavailable`
8. `browser_unavailable`

## 8. 可观测性（埋点）
建议事件：
1. `magician_entry_opened`
2. `magician_toggle_changed`
3. `magician_permission_prompt_shown`
4. `magician_intent_routed`
5. `magician_tool_executed`
6. `magician_tool_failed`

关键字段：
1. `intent`
2. `latency_ms`
3. `success`
4. `error_code`
5. `fallback_used`

## 9. 测试策略
### 9.1 Unit Test
1. Intent 解析。
2. Schema 校验。
3. 默认值推断。
4. 错误码映射。

### 9.2 Integration Test
1. 五个动作各一条成功路径。
2. 权限不足路径。
3. 依赖缺失路径（Chrome 不在、Shortcut 缺失、邮箱未配置）。

### 9.3 UI Test
1. 魔法师页面渲染。
2. 五个开关状态切换。
3. 权限引导弹窗。
4. text_transform 预览与替换流程。

## 10. 构建与发布建议
1. 本地先跑 build 与测试。
2. 测试通过后，再走项目自动发布脚本。
3. 仅提交本次文档与功能相关文件，避免夹带无关改动。

项目脚本（已约定）：

```bash
scripts/auto-ship.sh --message "feat: add magician module" --files <files...>
```

## 11. 渐进开发计划
1. `Milestone 1`：模块骨架 + 页面 + 开关 + mock。
2. `Milestone 2`：text_transform 全链路。
3. `Milestone 3`：web_search + create_event。
4. `Milestone 4`：create_note + compose_email_draft。
5. `Milestone 5`：埋点、测试补齐、发布。

## 12. 通过标准
1. 五个动作都可从同一入口触发。
2. 每个动作具备成功与失败反馈。
3. 权限不足时有明确引导。
4. text_transform 支持预览、替换、撤销。
5. 全量测试通过，主流程无回归问题。
